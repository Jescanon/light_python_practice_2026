# Журнал

Журнал нужен, чтобы кратко фиксировать:
- что сделано;
- что не получилось;
- что осталось на следующий этап.

## Формат записи
- дата;
- этап;
- короткий результат;
- проблемы;
- следующий шаг.

---

### 2026-06-05 13:30 - Этап 1. Каркас
- **Результат:** сделал каркас в src/. Точка входа main.py на argparse, есть команда scan.
  Заложил в sqlite таблицу scans (история запусков), база сама создаётся в data/. Логи идут
  в консоль и в файл. Заполнил мету, добавил .gitignore и README. Запуск проверил - работает,
  база появляется.
- **Проблемы:** забыл в main.py написать конкретный класс в except, стоял голый except: и ловил
  вообще всё, непонятно что падает. Поправил на except Exception.
  В config.py остался print, который светил пути каждый раз (Для дебага) - убрал.
- **Следующий шаг:** Этап 2 - рекурсивно обойти папку, собрать путь/размер/дату/тип, сохранить в
  sqlite и сделать простой фильтр.

### 2026-06-05 19:50 - Этап 2. Команда scan и парсер расширений
- **Результат:** добавил подкоманду scan в argparse (path, --ext, --name). Написал _parse_ext -
  бьёт строку ".py,txt" в множество, нормолизует регистр и точку. В config вынес SKIP_FILES
  (venv, .git, .idea, __pycache__)
- **Проблемы:** --ext принимал и "py", и ".py", сделал чтобы точка добавлялась сама.
- **Следующий шаг:** сам обход папки.

### 2026-06-05 20:00 - Этап 2. Рекурсивный обход
- **Результат:** написал walk через iterdir + рекурсию руками (сказали rglob нельзя). Спускается
  в подпапки, служебные пропускает (список в src/config), по каждому файлу собирает
  путь/размер/дату/расширение в rows.
- **Проблемы:** ловил относительный путь, считал relative_to от самого файла - выходила точка,
  потому что затер переменую path в цикле. Развел root и entry - починилось.
- **Следующий шаг:** фильтры и вывод.

### 2026-06-05 20:25 - Этап 2. Фильтры и вывод
- **Результат:** прикрутил фильтры --ext (по расширению) и --name (подстрока в имени), список
  файлов печатается. debug-лог "нашёл файл" только под -v.
- **Проблемы:** фильтры сначала стояли после print и ничего не фильтровали, перенес перед записю.
- **Следующий шаг:** запись в базу.

### 2026-06-05 20:40 - Этап 2. Запись в SQLite
- **Результат:** добавил в схему +1 таблицу files под индекс файлов (root, rel, size, mtime, ext,
  hash, ключ по root+rel). Соединение завернул в контекстный менеджер (commit/rollback/close сами).
  По каждому файлу сначала SELECT - есть ли уже такая запись: если есть, делаю REPLACE (обновляю),
  если нет - INSERT (добавляю), заодно считаю added и updated. Повторный скан не падает и не дублит, 
  знаю, что тут N+1 запрос, позже, если не забуду, переделаю чутка по другому.
- **Проблемы:** мучился. executemany стоял внутри цикла и пихал весь список - ловил
  IntegrityError UNIQUE.
- **Следующий шаг:** удаление пропавших файлов и тесты.

### 2026-06-05 21:15 - Этап 2. Удаление пропавших и тесты
- **Результат:** добавил удаление из индекса файлов, которых больше нет на диске (old минус то,
  что нашли на диске), и счётчик удалено. В конце скана пишу строку в scans (история запуска):
  когда сканил, сколько найдено/добавлено/изменено/удалено - теперь таблица scans не пустует.
  Написал тесты на unittest - обход, фильтр, повторный скан без дублей. Все 7 зелёные.
- **Проблемы:** при удалении сравнивал с отфильтрованным списком, из-за чего scan с --ext сносил
  из базы чужие файлы. Поправил - сравниваю с тем, что реально на диске.
- **Следующий шаг:** добавить команду показа индекса.

### 2026-06-05 21:18 - Этап 2. Команда list
- **Результат:** добавил подкоманду list - показывает, что уже в базе по папке, без нового скана.
  Печатает размер и путь, в конце общее число, при пустой базе подсказывет сделать scan.
- **Проблемы:** list искал по resolve()-пути, а scan писал root как ввели, без resolve. После
  scan с относительным путём list выдавал "Пусто". Поправил - добавил .resolve() в scan, теперь
  обе команды хранят и ищут по одному абсолютному пути.
- **Следующий шаг:** команда показа истории запусков.

### 2026-06-05 21:35 - Этап 2. Команда history
- **Результат:** добавил подкоманду history - читает таблицу scans и печатает запуски по папке
  (когда, сколько найдено/добавлено/изменено/удалено), от новых к старым. Теперь scans и пишется,
  и показывается.
- **Проблемы:** в build_parser забыл присвоить p = sub.add_parser("history"), из-за чего path
  уходил в парсер list, а history оставался без аргумента. Ещё history 
  проверял пустоту прямо на курсоре - добавил .fetchall(), забыл про него).
- **Следующий шаг:** Этап 3 - хэши файлов и поиск дубликатов.
